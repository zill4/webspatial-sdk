---
"@webspatial/react-sdk": patch
---

Fix head style synchronization for spatial windows (SpatialDiv/attachments) to avoid duplicated or stale stylesheet injection during rapid host `<head>` updates.

